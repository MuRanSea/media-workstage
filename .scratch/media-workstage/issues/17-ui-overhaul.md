# 17: UI 梳理：卡片精简、属性面板、画布操作、工程页与设置

**What to build:**
在保留深色风格的前提下理顺交互与层级：卡片只保留预览、提示词和生成按钮，详细参数移到选中卡片后出现的右侧属性面板；补齐画布的添加、删除、撤销、复制粘贴操作；精简顶栏；用应用内对话框替换浏览器原生弹窗；设置页统一文案并提示未保存的修改。

**Blocked by:** 16-project-ui-and-autosave

**Status:** resolved

- [x] 共用 UI 组件 `components/ui/`：Button、Segmented、Field/Section/Toggle、Dialog（Promise 版 confirm/prompt）、Toast（可带撤销）、Menu/MenuButton；最小字号 11px，文案统一为中文
- [x] 卡片外壳 `CardShell`：点任意位置选中、双击标题重命名、⋯ 菜单；预览叠加生成进度、点击查看大图（下载/新窗口）；错误可展开和复制；提示词自动增高
- [x] 右侧 `InspectorPanel`：图片、视频、文本三类参数；多选时显示对齐和网格排列（替代底部的 SelectionToolbar）；末尾可折叠查看请求 JSON
- [x] 参数规则抽到 `engine/cardParams.ts`，新卡片与复制逻辑抽到 `engine/cardFactory.ts`，均有单测
- [x] 新卡片放在视口中心并自动选中；双击或右键空白处在光标处添加；右键卡片可生成、复制、删除
- [x] 快捷键：Delete（带撤销提示）、Ctrl+Z / Ctrl+Shift+Z / Ctrl+Y、Ctrl+C / Ctrl+V（粘贴到光标处，重新编号 @图N 并保留内部连线）、Ctrl+D、Ctrl+A、V / H、?
- [x] 撤销历史 `engine/useHistory.ts`：输入合并为一步、拖动松手记一步；撤销时保留任务结果（`mergeTaskState`）
- [x] 顶栏精简为 logo + 工程名/保存状态/切换菜单 + 「添加」下拉 + 设置；导航条增加撤销/重做和快捷键说明，属性面板打开时自动让位
- [x] 修复：新卡片把占位文案当成真实提示词；旧工程加载时清掉这些遗留文案
- [x] 工程列表显示封面（后端 `Summary.cover`），重命名/删除走应用内对话框，列表页可打开设置
- [x] 设置弹窗：“演示模式”替代“测试桩模式 (Mock)”，分“连接 / 模型”两段，标出未保存的服务商，关闭时提示放弃修改

## Answer

前端 69 个单测、Go 全部测试通过。打包后在浏览器里走查：新建工程对话框、空画布引导、添加与自动选中、属性面板改参数后卡片摘要同步、fake 渠道生成与大图查看、Delete 后提示条撤销（生成结果保留）、双击添加菜单、拖线连接与面板里的参考图列表、全选复制粘贴的重新编号、右键菜单、多选排列与撤销、设置页未保存提示。
